{% extends 'base.html.twig' %}

{% block title %}Edit CV{% endblock %}

{% block body %}
    <h3 class="mb-3 mt-3 text-center">Modifier CV</h3>

    {{ include('cv/_form.html.twig', {'button_label': 'Modifier'}) }}

    <!--<a href="{{ path('c_v_index') }}">back to list</a>-->

    {{ include('cv/_delete_form.html.twig') }}


        {% endblock %}
